const sharp=require("sharp"),fs=require("fs"),path=require("path"),target=path.resolve(__dirname,"src/public/images"),destination=path.resolve(__dirname,"dist/images");fs.existsSync(destination)||fs.mkdirSync(destination),fs.readdirSync(target).forEach((e=>{sharp(`${target}/${e}`).resize(800).toFile(path.resolve(__dirname,`${destination}/${e.split(".").slice(0,-1).join(".")}-large.jpg`)),sharp(`${target}/${e}`).resize(480).toFile(path.resolve(__dirname,`${destination}/${e.split(".").slice(0,-1).join(".")}-small.jpg`))}));